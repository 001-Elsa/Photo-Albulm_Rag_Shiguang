# v1.0.0 工程化实验说明

定位升级:个人相册工具 → **企业媒资语义检索平台**(个人模式 = `auth_enabled=false` 的单机部署)。
企业场景:电商商品图库、制造业质检照片、设计素材库的自然语言检索——同一套技术,面向组织内多人使用。

## 一、本次改造内容

### 1. 存储抽象层(`vectorstore.py`)
- `LocalVectorStore`:内存矩阵,单机 10 万级(默认)
- `PgVectorStore`:PostgreSQL + pgvector、HNSW 索引的实验性适配；尚未完成大规模容量与并发验证
- 接口三个方法(`refresh/search/stats`),换 Milvus 只需再实现一个类
- 配置切换:`vector_backend: local | pgvector`;pgvector 连不上自动降级 local 并告警

### 2. schema 版本迁移(`migrations.py`)
- `schema_version` 落库,启动自动补齐落后版本,幂等可重入
- v0.8/v0.9 老库无损升级(实测:老数据 + 新表并存)
- 规矩:新变更只能追加,禁止修改历史迁移

### 3. 认证与 RBAC(`auth.py`,纯标准库)
- 口令:PBKDF2-HMAC-SHA256(26 万次迭代 + 随机盐)
- 会话:HMAC-SHA256 签名令牌(7 天过期),HttpOnly Cookie / Bearer 双通道
- 角色:`admin`(索引、设置、用户管理、审计)/ `viewer`(搜索、浏览)
- 首次启动自动创建 admin,初始密码写 `data/admin_initial_password.txt`(0600 权限)
- 用户管理 API:`POST /api/users`、`POST /api/password`、`GET /api/users`

### 4. 审计日志
- 登录(含失败)、搜索、原图查看、索引、设置变更全部落 `audit_log` 表
- `GET /api/audit`(admin)查看;这是企业合规的最低要求

### 5. 可观测性(`observability.py`)
- `GET /healthz`:存活 + schema 版本 + 向量索引状态(K8s liveness 可直接用)
- `GET /metrics`:Prometheus 文本格式——请求计数(路径归一化防高基数)、
  搜索延迟直方图、索引量 gauge
- 结构化 JSON 日志(`json_logs: true`),接 ELK/Loki 即用

### 6. 限流
- 令牌桶(按用户):默认突发 30、稳态 2 req/s,超限 429;参数可配

### 7. 12-factor 配置
- 所有配置项支持环境变量覆盖:`SHIGUANG_<字段大写>`(容器部署标准姿势)

### 8. 容器化与 CI
- `Dockerfile`(healthcheck 内置)、`docker-compose.yml`(可选 pgvector profile)
- GitHub Actions:3 个 Python 版本矩阵跑测试 + 编译检查 + Docker 构建

## 二、验证状态(诚实声明)

| 项 | 状态 |
|---|---|
| 认证/令牌/迁移/指标/限流/审计 | ✅ 有离线自动化测试，数量以当前测试运行结果为准 |
| 老库(v0.9)迁移 + 检索回归 | ✅ 端到端冒烟通过 |
| FastAPI 鉴权中间件、登录页 | ⚠️ 沙箱无 fastapi,代码评审级;你机器上启动后手工过一遍登录流程 |
| PgVectorStore、Docker、CI | ⚠️ 需要你机器上 Docker/Postgres 联调(命令见下) |

## 三、你机器上的联调清单

```bash
# 1. 基础模式(SQLite + 内存向量,和以前一样,多了登录)
python run.py     # 控制台会提示初始密码文件位置

# 2. Docker 单机
docker compose up -d --build

# 3. pgvector 企业模式
docker compose --profile pg up -d
SHIGUANG_VECTOR_BACKEND=pgvector SHIGUANG_PG_DSN=postgresql://shiguang:shiguang@localhost:5432/shiguang python run.py

# 4. 指标
curl http://127.0.0.1:8626/healthz
curl http://127.0.0.1:8626/metrics
```

## 四、简历写法(这一阶段新增的话术)

> - 设计可插拔向量存储层(内存矩阵/实验性 pgvector-HNSW)，支持配置切换和故障降级；具体容量以真实压测为准
> - 实现完整企业基线:PBKDF2+签名令牌认证、RBAC、审计日志、令牌桶限流、Prometheus 指标、结构化日志、schema 版本迁移、Docker/CI

## 五、阶段 2~4 路线(未动工)

- **阶段 2 规模化**:索引 Worker 独立进程 + 任务队列(Redis/RQ)、对象存储(MinIO/S3)、Milvus 适配、缩略图 CDN
- **阶段 3 企业功能**:部门/空间隔离、SSO(OIDC)、细粒度权限、数据保留策略
- **阶段 4 可用性证明**:locust 压测报告(QPS/P99)、备份恢复演练、灰度发布
